source "https://rubygems.org"

gem "jekyll", "~> 3.9"

# GitHub Pages plugin (matches GitHub's build environment)
gem "github-pages", group: :jekyll_plugins

# If you're using just-the-docs via remote_theme:
gem "just-the-docs"